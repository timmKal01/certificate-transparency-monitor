{
    "name": "certificate-transparency-monitor",
    "version": "0.1.0",
    "type": "module",
    "description": "Search Certificate Transparency logs for newly issued SSL/TLS certificates on a domain via crt.sh.",
    "engines": {
        "node": ">=18.0.0"
    },
    "dependencies": {
        "apify": "^3.2.6"
    },
    "scripts": {
        "start": "node src/main.js"
    }
}
